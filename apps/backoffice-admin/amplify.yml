version: 1
applications:
  - appRoot: apps/backoffice-admin
    frontend:
      phases:
        preBuild:
          commands:
            - npm install -g pnpm
            - cd ../..
            - pnpm install
        build:
          commands:
            - cd apps/backoffice-admin
            - pnpm run build
      artifacts:
        baseDirectory: apps/backoffice-admin
        files:
          - '.next/**/*'
          - 'public/**/*'
          - 'package.json'
          - 'node_modules/**/*'
      cache:
        paths:
          - ../../node_modules/**/*
          - .next/cache/**/*
