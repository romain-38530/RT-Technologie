# pnpm configuration for monorepo
# Enable public hoisting for better compatibility with AWS Amplify
public-hoist-pattern[]=*

# Shamefully hoist to create a flat node_modules structure
shamefully-hoist=true

# Auto install peers
auto-install-peers=true
